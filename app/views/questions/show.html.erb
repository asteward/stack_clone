<h2><%= @question.text %></h2>
<hr>

<ul>
  <% @question.answers.each do |answer| %>
    <p><%= User.find(answer.user_id).name %>: <%= answer.text %></p>
  <% end %>
<ul>

<%= link_to "Answer", new_question_answer_path(@question), class: "btn btn-success" %>
